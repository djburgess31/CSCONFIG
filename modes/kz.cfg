echo ========================================================
echo                      kz
comp
bind "SHIFT" sm_checkpoint
bind "CTRL" sm_gocheck
bind "CAPSLOCK" sm_prev
bind "TAB" sm_start
bind "q" sm_next
bind "w" sm_undo
bind "r" sm_hide
bind "t" sm_options
echo ========================================================